import Foundation

enum ViewNameType: String {
    case signInView = "サインイン画面"
    case nameRegistrationView = "名前登録画面"
    case iconRegistrationView = "アイコン登録画面"
    case cookPostView = "料理投稿画面"
    case addPostView = "投稿追加画面"
    case postDetailView = "投稿詳細画面"
    case myPageView = "マイページ画面"
}

enum ButtonNameType: String {
    case likeButton = "ライクボタン"
}
